[pytest]
testpaths = tests
markers =
    unit: Unit tests (no LLM or network calls)
    integration: Integration tests (mocked LLM calls)
    slow: Slow tests (live API calls)
addopts = -v --tb=short
